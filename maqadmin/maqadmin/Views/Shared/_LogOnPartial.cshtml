@if(Request.IsAuthenticated) {
    <text>Bienvenido local <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Salir", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Ingresar", "LogOn", "Account") ]
}
